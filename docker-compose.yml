services:
  api:
    build: .
    ports:
      - "${PORT:-8080}:8080"
    env_file: .env
    environment:
      PORT: 8080
    volumes:
      # Mantém a cópia local das tabelas entre reinícios do container.
      - cache:/app/.cache
    restart: unless-stopped

volumes:
  cache:
